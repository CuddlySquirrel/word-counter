default: &default
  adapter: postgresql
  encoding: unicode
  database: rails-word-count
  pool: 5
  timeout: 5000
  username: <%= ENV['USER'] %>

development:
  <<: *default
  database: development

test:
  <<: *default
  database: test

production:
  <<: *default
  database: production
